name = "family-assistant"
main = "worker.js"
compatibility_date = "2026-09-01"

# 绑定到你自己的子域名（域名已经在 Cloudflare，所以不用迁 DNS）
[[routes]]
pattern = "api.wangyuyue.xyz"
custom_domain = true

# 语音识别用的 Workers AI 绑定（免费计划每天 10,000 Neurons，约 214 分钟音频）
[ai]
binding = "AI"
