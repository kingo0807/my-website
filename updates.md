---
layout: article
title: "更新记录"
description: "这个网站每次能被看见的变化，按日期倒序记录。"
permalink: /updates/
---

# 更新记录

这里只记「打开网站能察觉到」的变化。开发过程记录在仓库的 `NOTES.md` 里，那个文件不对外发布。

## 2026-09-23

**手机助手：公式不再是反斜杠，输入框也修好了**

- 回答里的数学公式会转成能读的样子：`\(I=\int_0^\infty e^{-x^2}dx\)` 现在显示为 `I=∫₀^∞ e^(-x²)dx`，∫ ∞ π θ √ 和上下标都会转；代码块里的公式原样保留，Windows 路径（`C:\Users\…`）不受影响；
- 输入框旁边加了语音按钮之后，手机上的提示文字一度被挤到框外；现在窄屏下按钮收窄、提示文字缩短，排版恢复正常。

**文章页更好读了**

- 长文右侧有了本页目录（标题太少就不显示），滚到哪里高亮哪里；
- 代码块右上角可以一键复制；
- 右上角能手动切换深色/浅色，默认跟随系统；
- 顶部有阅读进度条，右下角有回到顶部按钮。

**手机助手：能存多段对话、能说话、能朗读**

- 顶部 ☰ 打开「历史对话」，最多留 20 条，点一条接着聊，✕ 删掉；
- 输入框旁多了 🎤，可以直接说话提问（浏览器支持时才出现）；
- 每条回答下面多了「朗读」，眼睛累了可以听；
- 家庭后端加了每日额度（默认每台设备 200 次），免得链接被外人拿去刷。

**新增这一页和仓库自检**

- 就是你现在看的「更新记录」页；
- 仓库加了 GitHub Actions：每次提交自动跑助手自检和站点链接检查。

**不存在的地址有中文页面了**

- 以前访问错误地址会看到 GitHub 的英文默认页；现在是本站的 404 页，并列出全部文章。

**浏览器标签有图标了**

- 新增站点图标 `assets/favicon.svg`（蓝色圆角方块 + 白色 K），首页和所有文章页都已引用。

**搜索结果与分享卡片**

- 每个页面补上了描述文字、canonical 和 `og:*` 信息，分享到聊天软件里能显示标题与摘要。

**`robots.txt` 与 `sitemap.xml`**

- 以前 `/robots.txt` 返回 404；现在两者都在，站点地图会跟着文章自动更新。

**文章目录顺序修正**

- 目录页和站点地图里的文章顺序改为按 front matter 的 `guide_order` 排：
  Git → teedoc → 域名与上线 → 后端 → 发布与回滚。
